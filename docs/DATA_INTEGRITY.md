# DATA_INTEGRITY — 事実値の正典

> 料金・還元・補償・仕様の**唯一の正典**。UI / seed / トークの数値はここと一致させる。
> 不確かな値は推測で埋めず「要確認」表示＋`docs/AUDIT.md` に記録。出典は公式URL＋確認日で管理。

出典は `src/data/seed.ts` の `PRODUCTS`（dカード GOLD / PLATINUM）とトークスクリプト、`supabase/seed.sql`。
行番号はリファクタでズレるため定数名で参照する。値は下表が正典。

## dカード GOLD（年会費 11,000円）

| 項目         | 正典値                                                       |
| ------------ | ------------------------------------------------------------ |
| ケータイ割引 | 最大550円/月＝**年間約6,600円**                              |
| ポイント還元 | 対象利用料金10%＝例 約1,000P/月＝**年間約12,000P**           |
| **合計訴求** | **割引約6,600円 ＋ ポイント約12,000P ＝ 年間約18,600円相当** |
| 実質メリット | 年会費11,000円でも 18,600 − 11,000 で実質プラス              |

> 内訳（6,600 / 12,000）と合計（18,600）の整合を必ず保つ。片方だけ変えない。

## dカード PLATINUM 切替

| 項目           | 正典値                                                     |
| -------------- | ---------------------------------------------------------- |
| 年会費相殺     | GOLD年会費 11,000円が相殺                                  |
| **実質コスト** | **実質18,700円**で初年度お試し（合わなければGOLDに戻せる） |

## 出典・鮮度管理

- 各 `products.official_url` と `products.official_checked_at`（YYYY-MM-DD）を正とする。
- `official_checked_at` から **90日超で鮮度警告**（Phase 2 の管理画面と連動）。
- 値を更新したら確認日も更新し、`product_versions` に before/after を記録。

## 不明値の扱い

- 確定できない料金・補償・仕様は**それらしい数字を置かない**。
- UI に「**要確認**」を表示し、本 docs と `docs/AUDIT.md` に TODO を残す。
- 判断に迷う事実は止めて高橋に確認する。

## 要確認リスト（公式URL/確認日の実体精査が必要）

- [ ] `supabase/seed.sql` / `src/data/seed.ts` の各商材 `official_url`・`official_checked_at` が実在URL・実日付か（現状 seed のため要精査）。詳細は AUDIT.md A-D1 参照。
